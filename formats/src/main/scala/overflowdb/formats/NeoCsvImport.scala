package overflowdb.formats

import com.github.tototoshi.csv._
import overflowdb.Graph
import java.nio.file.Path

object NeoCsvImport extends Importer {

  override def runImport(inputFile: Path, graph: Graph): Unit = {
    ???
  }

}
